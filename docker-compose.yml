version: "2"
services:
  app:
    container_name: benign-filter-container
    build:
      context: ./
      dockerfile: Dockerfile
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - 8080:80
      
networks:
  default:
    external:
      name: benign-filter-network
